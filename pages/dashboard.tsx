import type { NextPage } from 'next';

const Dashboard: NextPage = () => {
  return (
    <div>
      <h1 className="text-3xl font-default font-bold">Dashboard</h1>
    </div>
  );
};

export default Dashboard;